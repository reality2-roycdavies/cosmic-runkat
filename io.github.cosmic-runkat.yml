app-id: io.github.cosmic-runkat
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: cosmic-runkat

finish-args:
  # Wayland/X11 display access
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  # Session D-Bus access for system tray
  - --socket=session-bus
  # System tray via StatusNotifierItem
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.StatusNotifierWatcher
  # Host filesystem for /proc/stat CPU monitoring (required for system monitoring)
  - --filesystem=host:ro
  # App config storage
  - --filesystem=~/.config/cosmic-runkat:create

modules:
  - name: cosmic-runkat
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/cosmic-runkat/cargo
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      - cargo --offline build --release --verbose
      - install -Dm755 target/release/cosmic-runkat /app/bin/cosmic-runkat
      - install -Dm644 resources/io.github.cosmic-runkat.desktop /app/share/applications/io.github.cosmic-runkat.desktop
      - install -Dm644 resources/io.github.cosmic-runkat.metainfo.xml /app/share/metainfo/io.github.cosmic-runkat.metainfo.xml
      - install -Dm644 resources/io.github.cosmic-runkat.svg /app/share/icons/hicolor/scalable/apps/io.github.cosmic-runkat.svg
      - install -Dm644 resources/io.github.cosmic-runkat-symbolic.svg /app/share/icons/hicolor/symbolic/apps/io.github.cosmic-runkat-symbolic.svg
    sources:
      - type: git
        url: https://github.com/reality2-roycdavies/cosmic-runkat.git
        tag: v0.2.2
      - cargo-sources.json
